import 'package:flutter/material.dart';

/// Placeholder screen scaffolded in Phase 1 — real content lands in its
/// dedicated phase (see docs/MOBILE_PLAN.md).
class BoardsPage extends StatelessWidget {
  const BoardsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('BoardsPage'));
  }
}
